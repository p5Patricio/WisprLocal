# Skill Registry — WisprLocal

> Auto-generated by sdd-init. Last updated: 2026-04-06.

## Project Conventions

- **Convention files**: none at project level
- **Agent config**: `~/.claude/CLAUDE.md` (user-level)
- **Stack**: Python 3.12.7, Windows-only, CUDA GPU
- **Venv**: `.venv/Scripts/python.exe`

## User Skills

| Skill | Trigger Context |
|-------|----------------|
| `branch-pr` | Creating a pull request or PR |
| `issue-creation` | Creating a GitHub issue, reporting a bug |
| `judgment-day` | Adversarial review — "judgment day", "dual review", "juzgar" |
| `skill-creator` | Creating new skills or agent instructions |

## SDD Skills (auto-loaded by orchestrator)

`sdd-explore`, `sdd-propose`, `sdd-spec`, `sdd-design`, `sdd-tasks`, `sdd-apply`, `sdd-verify`, `sdd-archive`

## Compact Rules

### Python / Windows Desktop App
- Always use `.venv/Scripts/python.exe` — never system python
- Windows-only APIs: `winsound`, `pynput`, `pystray` — no cross-platform fallbacks needed
- GPU operations: wrap in try/except, always call `gc.collect()` + `torch.cuda.empty_cache()` after unload
- Inject text via `pyperclip` + simulated `Ctrl+V` (handles Spanish accents correctly)

### General
- No test runner — verify manually on Windows
- Conventional commits only, no AI attribution
- Never build after changes
